# Defaults used only until Settings are saved in the app.
# The app stores live config in:
#   ~/Library/Application Support/gfx-pkg-exporter/settings.json

SOCKET_IO_URL=
WATCH_FOLDER=/path/to/watch/folder
RENDER_FOLDER=/path/to/renders
CDN_URL=https://cdn.example.com
NEXRENDER_WORKPATH=/tmp/nexrender
AERENDER_PATH=/Applications/Adobe After Effects 2026/aerender
DEBUG=false
